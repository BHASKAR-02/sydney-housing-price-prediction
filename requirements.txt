# Runtime dependencies for the deployed application.
#
# This is the file Streamlit Community Cloud installs from, so it is kept lean on
# purpose. It has what the Streamlit app and the FastAPI backend need and nothing
# else. Installing jupyter and matplotlib here would roughly triple the build time
# on a free tier for libraries the running app never imports.
#
# To run the notebook as well:  pip install -r requirements-dev.txt

streamlit==1.50.0
requests==2.32.3

fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.5

pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
joblib==1.5.3
