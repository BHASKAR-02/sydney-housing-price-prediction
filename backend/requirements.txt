# Backend only, for the Render service. No streamlit, no notebook libraries.
# Ranges rather than exact pins for the same reason as the root requirements.txt:
# the host picks its own Python and an unmatched pin means a source build.

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.9,<3
pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2
