# Everything the app needs, plus what is required to run the notebook and
# regenerate the figures. Local use, not for the deployment host.

-r requirements.txt

matplotlib>=3.8,<4
seaborn>=0.13,<1
jupyter>=1.0
notebook>=7.0
nbconvert>=7.0
